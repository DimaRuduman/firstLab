// З сигнатурою inc(n: number): number, приклад виклику: const a = 5; const b = inc(a); console.dir({a, b});


const a = 5;
const b = inc(a);

function inc(n) {
    return n + 1;
}

console.dir({ a, b });






